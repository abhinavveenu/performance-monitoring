{
  "name": "perf-dashboard-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for Performance Monitoring Dashboard (SDK, API, Workers, Dashboard, Infra)",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws --if-present dev",
    "lint": "npm run -ws --if-present lint",
    "format": "npm run -ws --if-present format",
    "typecheck": "npm run -ws --if-present typecheck"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}

